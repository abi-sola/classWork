const age = 18;

//using ternary operator to check if age is 18 or above
const canVote = (age >= 18) ? 'Yes, you can vote.' : 'No, you cannot vote yet.';
console.log(canVote); //Output:Yes, you can vote.